{
  "name": "zone",
  "version": "1.0.0",
  "private": true,
  "description": "Root package.json to run both backend and frontend together",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}